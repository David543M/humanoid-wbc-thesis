# Experiments

Iterative experimental work. Each experiment gets a dated subfolder under `logs/` or `results/`.

Naming: `YYYY-MM-DD_experiment-name/`.

Recommended contents per experiment:
- `config.yaml` — exact config used
- `seed.txt` — RNG seeds
- `metrics.csv`
- `notes.md` — what was tested and why
